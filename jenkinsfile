pipeline {
    agent {
        node {
            label "linux && java11" // This will run on a node with the label "linux && java11"
        }
    }

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
